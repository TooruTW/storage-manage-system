const LoadingView = () => {
  return (
    <div className="w-full h-full flex justify-center items-center fixed top-0 left-0 bg-primary/50 backdrop-blur-md z-50">
      <h1 className="text-h1 text-white">Loading...</h1>
    </div>
  );
};

export default LoadingView;
